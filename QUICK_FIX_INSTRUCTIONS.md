# 🚀 БЫСТРАЯ ИНСТРУКЦИЯ: Компиляция и тест

## ЧТО МЫ ИСПРАВИЛИ

**ПРОБЛЕМА:** Траектория "убегала" когда дрон стоял на месте (моторы выключены)

**РЕШЕНИЕ:** 
1. ✅ Детектируем что дрон не движется
2. ✅ Перепланируем от РЕАЛЬНОЙ позиции
3. ✅ Траектория теперь "прилипает" к дрону

---

## ШАГИ

### 1. Компиляция (2 минуты)
```bash
cd ~/ws_ego_planner
catkin_make
source devel/setup.bash
```

### 2. Запуск (1 минута)
```bash
roslaunch ego_planner run_in_sim.launch  # или ваш launch
```

### 3. В RViz добавьте топики:
- `/ego_planner_node/debug/current_odom` (Marker, зелёный)
- `/ego_planner_node/debug/planned_position` (Marker, красный)
- `/ego_planner_node/debug/dense_trajectory` (Marker, оранжевый)

### 4. Дайте цель дрону
- 2D Nav Goal в RViz
- Или через ваш интерфейс

---

## ЧТО ОЖИДАТЬ

### ✅ С ВЫКЛЮЧЕННЫМИ МОТОРАМИ:
```
🟢 Зелёный маркер - стоит на месте
🔴 Красный маркер - НЕ убегает дальше 1 метра
🟠 Оранжевая траектория - "возвращается" к дрону каждую секунду

В терминале:
[ WARN] 🛑 Drone not moving but trajectory running! Error: 1.05m, vel: 0.02m/s
[ WARN] 🔧 Using REAL odometry for replanning (drone not moving, error: 1.05m)
```

### ✅ С ВКЛЮЧЁННЫМИ МОТОРАМИ (в полёте):
```
🟢 Зелёный маркер - движется
🔴 Красный маркер - рядом с зелёным (< 30см)
🟠 Оранжевая траектория - плавная

В терминале:
Нет предупреждений (всё работает нормально)
```

---

## НАСТРОЙКА ПАРАМЕТРОВ

Если траектория всё ещё "убегает", уменьшите пороги в коде:

### Файл: `src/planner/plan_manage/src/ego_replan_fsm.cpp`

**Строка ~305** (в EXEC_TRAJ):
```cpp
if (tracking_error > 1.0 && velocity_magnitude < 0.1)
     // ↑ МЕНЬШЕ = чаще перепланирование (попробуйте 0.5)
```

**Строка ~375** (в planFromCurrentTraj):
```cpp
if (tracking_error > 0.8 && velocity_magnitude < 0.15)
     // ↑ МЕНЬШЕ = чаще использует одометрию (попробуйте 0.5)
```

---

## ПРОВЕРКА РАБОТОСПОСОБНОСТИ

### Тест 1: Дрон стоит на месте
- [x] Зелёный маркер на месте
- [x] Красный НЕ убегает > 1м
- [x] В логах "Drone not moving"
- [x] Траектория перепланируется

### Тест 2: Дрон летит
- [x] Зелёный и красный рядом
- [x] Нет предупреждений в логах
- [x] Плавный полёт

---

## ЕСЛИ ПРОБЛЕМЫ

### Проблема: Траектория всё равно убегает
**Решение:** Уменьшите `tracking_error > 1.0` до `0.5`

### Проблема: Слишком частые перепланирования
**Решение:** Увеличьте `tracking_error > 1.0` до `1.5`

### Проблема: Дрон дёргается
**Решение:** Увеличьте `blend` коэффициент с `0.6` до `0.4` (больше плановой траектории)

---

## 🎯 ГОТОВО!

Теперь траектория правильно привязана к дрону и не "убегает"!

Если есть вопросы - напишите что видите в RViz и в логах.
