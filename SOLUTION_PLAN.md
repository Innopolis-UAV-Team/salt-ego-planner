# План решения проблемы визуализации и перепланирования

## 🎯 РЕКОМЕНДУЕМЫЙ ПОДХОД: Поэтапный с диагностикой

---

## ✅ **ШАГ 1: ДИАГНОСТИЧЕСКАЯ ВИЗУАЛИЗАЦИЯ** (УЖЕ РЕАЛИЗОВАНО!)

### Что сделано:
Добавлена визуализация для **диагностики реальной проблемы**:

1. **🟢 Зелёный маркер** - где дрон **РЕАЛЬНО** находится (из одометрии)
2. **🔴 Красный маркер** - где дрон **ДОЛЖЕН** быть (из траектории по времени)
3. **🟠 Оранжевая линия** - плотная визуализация траектории (шаг 50мс)

### Новые топики в RViz:
```
/ego_planner_node/debug/current_odom       # Зелёный - реальная позиция
/ego_planner_node/debug/planned_position   # Красный - плановая позиция
/ego_planner_node/debug/dense_trajectory   # Оранжевый - вся траектория
```

### Как использовать:
1. Скомпилируйте проект: `catkin_make`
2. Запустите планировщик
3. В RViz добавьте топики из списка выше (тип: Marker)
4. **СМОТРИТЕ НА РАЗНИЦУ** между зелёным и красным маркерами!

### Что покажет диагностика:

#### **Сценарий А: Зелёный и красный почти совпадают**
- ✅ Дрон **следует** за траекторией хорошо
- ✅ Контроллер работает правильно
- ❌ Проблема только в **грубой визуализации контрольных точек**
- 💡 **Решение**: Используйте оранжевую плотную визуализацию вместо красных точек

#### **Сценарий Б: Зелёный отстаёт от красного**
- ❌ Дрон **не успевает** за траекторией
- ⚠️ Возможные причины:
  - Слишком агрессивная траектория (высокая скорость/ускорение)
  - Проблемы с контроллером
  - Задержки в одометрии
- 💡 **Решение**: Смотрите ШАГ 2

#### **Сценарий В: Зелёный "прыгает" или шумит**
- ❌ Проблема с **одометрией** (лидар)
- 💡 **Решение**: Проверить качество одометрии, добавить фильтрацию

---

## 🔧 **ШАГ 2: АДАПТИВНОЕ ПЕРЕПЛАНИРОВАНИЕ** (если нужно)

Если диагностика показала **Сценарий Б** (дрон отстаёт), используйте это решение.

### Вариант 2А: Умная проверка отклонения (РЕКОМЕНДУЮ)

Вместо перепланирования каждые 1.5м, проверяем **реальное отклонение**:

```cpp
// В ego_replan_fsm.cpp, в case EXEC_TRAJ:

// Вычислить отклонение реальной позиции от плановой
double tracking_error = (odom_pos_ - pos).norm();

// Параметр из конфига (например, 0.5м)
double max_tracking_error = 0.5;  

if (tracking_error > max_tracking_error) {
  ROS_WARN("Tracking error too large: %.2f m, replanning!", tracking_error);
  changeFSMExecState(REPLAN_TRAJ, "TRACKING_ERROR");
}
```

**Преимущества:**
- ✅ Перепланирование только когда **РЕАЛЬНО нужно**
- ✅ Не увеличивает нагрузку если дрон летит хорошо
- ✅ Автоматически адаптируется к качеству полёта
- ✅ Можно настроить порог через launch-файл

**Нагрузка:** Минимальная (одна проверка каждые 10мс)

---

### Вариант 2Б: Использовать одометрию для перепланирования

**ВНИМАНИЕ:** Это более сложное изменение!

```cpp
bool EGOReplanFSM::planFromCurrentTraj()
{
  LocalTrajData *info = &planner_manager_->local_data_;
  ros::Time time_now = ros::Time::now();
  double t_cur = (time_now - info->start_time_).toSec();

  // ВАРИАНТ 1: Из траектории (текущий)
  // start_pt_ = info->position_traj_.evaluateDeBoorT(t_cur);
  
  // ВАРИАНТ 2: Из реальной одометрии (новый)
  start_pt_ = odom_pos_;  // ⚠️ Может вызвать разрывы!
  start_vel_ = odom_vel_;
  
  // Ускорение лучше брать из траектории для плавности
  start_acc_ = info->acceleration_traj_.evaluateDeBoorT(t_cur);

  bool success = callReboundReplan(false, false);
  // ...
}
```

**Проблемы:**
- ❌ Может создать **разрыв** в траектории
- ❌ Нужна проверка на **непрерывность**
- ❌ Требует глубокого тестирования

**Решение проблемы разрывов:**
```cpp
// Проверить расстояние
double discontinuity = (odom_pos_ - info->position_traj_.evaluateDeBoorT(t_cur)).norm();

if (discontinuity < 0.3) {  // Порог 30см
  start_pt_ = odom_pos_;  // Используем реальную позицию
} else {
  // Интерполяция для плавности
  double blend = 0.7;  // 70% реальной, 30% плановой
  start_pt_ = blend * odom_pos_ + (1.0 - blend) * info->position_traj_.evaluateDeBoorT(t_cur);
}
```

---

## 🎛️ **ШАГ 3: НАСТРОЙКА ПАРАМЕТРОВ** (всегда полезно)

### Параметры для экспериментов в `advanced_param.xml`:

```xml
<!-- Текущие значения -->
<param name="fsm/thresh_replan" value="1.5" type="double"/>
<param name="fsm/thresh_no_replan" value="1.0" type="double"/>

<!-- Попробуйте более частое перепланирование -->
<param name="fsm/thresh_replan" value="0.8" type="double"/>  <!-- Было 1.5 -->
<param name="fsm/thresh_no_replan" value="0.5" type="double"/>  <!-- Было 1.0 -->

<!-- Или добавьте новый параметр для отклонения -->
<param name="fsm/max_tracking_error" value="0.5" type="double"/>
```

### Параметры скорости/ускорения:

```xml
<!-- Если дрон не успевает, уменьшите -->
<arg name="max_vel" value="2.0" />  <!-- Попробуйте 1.5 -->
<arg name="max_acc" value="3.0" />  <!-- Попробуйте 2.0 -->
```

---

## 🚦 **РЕКОМЕНДУЕМАЯ ПОСЛЕДОВАТЕЛЬНОСТЬ ДЕЙСТВИЙ**

### **День 1: Диагностика** ✅ (УЖЕ ГОТОВО!)
1. ✅ Скомпилировать с новой визуализацией
2. ✅ Запустить полёт
3. ✅ Смотреть на разницу зелёный/красный в RViz
4. ✅ Записать rosbag для анализа

### **День 2: Анализ**
1. Проанализировать rosbag:
   ```bash
   rostopic echo /ego_planner_node/debug/current_odom
   rostopic echo /ego_planner_node/debug/planned_position
   ```
2. Вычислить среднее отклонение
3. Определить сценарий (А, Б или В)

### **День 3: Решение**

#### Если **Сценарий А** (всё хорошо, проблема только визуализация):
- ✅ Используйте оранжевую плотную траекторию
- ✅ Возможно добавить `optimal_list` в игнор в RViz
- ✅ **НЕ НУЖНО** менять алгоритм!

#### Если **Сценарий Б** (дрон отстаёт):
- ⚠️ Сначала попробуйте **уменьшить скорость** (max_vel)
- ⚠️ Затем добавьте **проверку отклонения** (Вариант 2А)
- ⚠️ Только в крайнем случае - использовать одометрию (Вариант 2Б)

#### Если **Сценарий В** (шумная одометрия):
- 🔧 Проверить настройки лидара
- 🔧 Добавить фильтр Калмана для одометрии
- 🔧 Увеличить `depth_filter_tolerance` в grid_map

---

## 📊 **СРАВНЕНИЕ РЕШЕНИЙ**

| Решение | Сложность | Риск | Эффект | Нагрузка |
|---------|-----------|------|--------|----------|
| **Диагностика (ШАГ 1)** | 🟢 Низкая | ✅ Нет | 🔍 Понимание | 0% |
| **Уменьшить порог (0.2м)** | 🟢 Низкая | ⚠️ Средний | ⚠️ Нестабильность | +700% |
| **Проверка отклонения (2А)** | 🟡 Средняя | 🟢 Низкий | ✅ Адаптивность | +0-200% |
| **Использовать одометрию (2Б)** | 🔴 Высокая | 🔴 Высокий | ⚠️ Разрывы | +0% |
| **Уменьшить скорость** | 🟢 Низкая | ✅ Нет | ✅ Стабильность | -20% |

---

## 💡 **МОЯ РЕКОМЕНДАЦИЯ**

### **План на неделю:**

**Понедельник (2 часа):**
1. ✅ Скомпилировать проект с диагностикой
2. ✅ Запустить тесты, смотреть визуализацию
3. ✅ Записать rosbag (10-15 минут полёта)

**Вторник (1 час):**
1. Проанализировать rosbag
2. Посчитать статистику отклонений
3. Определить реальную проблему

**Среда (2-4 часа):**
1. **Если отклонение < 20см**: Просто используйте плотную визуализацию, всё работает!
2. **Если отклонение 20-50см**: Добавьте проверку отклонения (Вариант 2А)
3. **Если отклонение > 50см**: Уменьшите max_vel, проверьте одометрию

**Четверг:**
1. Тестирование решения
2. Настройка параметров

**Пятница:**
1. Финальные тесты
2. Документация

---

## 🎯 **КРИТЕРИИ УСПЕХА**

### Минимальный успех:
- ✅ Видна реальная позиция vs плановая
- ✅ Понятна причина проблемы

### Хороший результат:
- ✅ Отклонение < 30см в 90% времени
- ✅ Плавный полёт без рывков
- ✅ Нагрузка CPU < 50%

### Отличный результат:
- ✅ Отклонение < 15см
- ✅ Адаптивное перепланирование по необходимости
- ✅ Красивая визуализация для демонстрации

---

## 🔧 **БЫСТРЫЕ ФИКСЫ (если совсем плохо)**

### 1. Самый быстрый способ (1 минута):
```xml
<!-- В advanced_param.xml уменьшить скорость -->
<arg name="max_vel" value="1.0" />  <!-- Было 2.0 -->
```

### 2. Чуть медленнее (5 минут):
```xml
<!-- Более частое перепланирование -->
<param name="fsm/thresh_replan" value="0.5" type="double"/>
```

### 3. Компромисс (30 минут):
- Добавить проверку отклонения (Вариант 2А)
- Параметр настраивается через launch

---

## 📝 **СЛЕДУЮЩИЕ ШАГИ**

1. ✅ **ПРЯМО СЕЙЧАС**: Скомпилируйте и запустите
2. 🔍 **ЧЕРЕЗ ЧАС**: Посмотрите на зелёный/красный маркеры
3. 📊 **ЗАВТРА**: Проанализируйте данные
4. 🔧 **ПОСЛЕЗАВТРА**: Примените нужное решение

---

## ❓ **ЧАСТЫЕ ВОПРОСЫ**

**Q: А если я просто уменьшу thresh_replan до 0.2м?**
A: ❌ Не рекомендую! Нагрузка вырастет в 7 раз, может зависнуть планировщик.

**Q: Безопасно ли использовать одометрию для перепланирования?**
A: ⚠️ Только с проверкой непрерывности и плавным смешиванием. Иначе будут рывки.

**Q: Что если проблема в одометрии?**
A: 🔍 Диагностика это покажет (зелёный маркер будет "прыгать"). Тогда нужно улучшать лидар/фильтрацию.

**Q: Сколько займёт полное решение?**
A: 📅 От 2 часов (если повезёт) до 2 дней (если нужны глубокие изменения).

---

**Готовы начать? Запустите с диагностикой и напишите что увидели!** 🚀
