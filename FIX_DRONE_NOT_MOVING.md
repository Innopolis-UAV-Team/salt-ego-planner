# Исправление: Траектория "убегает" когда дрон не движется

## 🎯 ПРОБЛЕМА

### Что происходило:
```
Дрон стоит на месте (моторы выключены / не в OFFBOARD)
    ↓
🟢 Зелёный маркер (реальная позиция) - СТОИТ
🔴 Красный маркер (плановая позиция) - УЛЕТАЕТ по времени
🟠 Оранжевая траектория - тоже УЛЕТАЕТ
    ↓
Траектория "убежала" от дрона на несколько метров!
```

### Почему это плохо:
1. ❌ Визуализация не показывает реальную ситуацию
2. ❌ Когда дрон включится, он будет пытаться "догнать" убежавшую траекторию
3. ❌ Возможны резкие движения или ошибки

---

## ✅ РЕШЕНИЕ

Мы добавили **двухуровневую защиту**:

### **Уровень 1: Детектирование застревания дрона**

В `case EXEC_TRAJ` добавлена проверка:

```cpp
// Вычисляем отклонение и скорость
double tracking_error = (odom_pos_ - pos).norm();
double velocity_magnitude = odom_vel_.norm();

// Если траектория убежала > 1м И дрон почти не двигается (< 0.1 м/с)
if (tracking_error > 1.0 && velocity_magnitude < 0.1) {
    ROS_WARN("🛑 Drone not moving but trajectory running!");
    // Экстренное перепланирование
    changeFSMExecState(REPLAN_TRAJ, "DRONE_NOT_MOVING");
}
```

**Что это даёт:**
- ✅ Если дрон застрял, траектория **НЕ УБЕЖИТ** больше чем на 1 метр
- ✅ Автоматически запускается перепланирование
- ✅ Предупреждение в логах (throttled - раз в секунду)

---

### **Уровень 2: Умное использование одометрии при перепланировании**

В функции `planFromCurrentTraj()` добавлена **трёхуровневая логика**:

#### **Случай A: Дрон застрял** (error > 0.8м, скорость < 0.15 м/с)
```cpp
start_pt_ = odom_pos_;      // ✅ Используем РЕАЛЬНУЮ позицию
start_vel_ = odom_vel_;     // ✅ Реальную скорость (≈ 0)
start_acc_.setZero();       // ✅ Ускорение = 0
```

**Результат:** Новая траектория начнётся **ТОЧНО** от текущего положения дрона!

---

#### **Случай B: Умеренное отклонение** (0.3м < error < 0.8м)
```cpp
double blend = 0.6;  // 60% реальной, 40% плановой
start_pt_ = blend * odom_pos_ + (1.0 - blend) * planned_pos;
start_vel_ = blend * odom_vel_ + (1.0 - blend) * planned_vel;
```

**Результат:** Плавный переход, избегаем резких скачков траектории

---

#### **Случай C: Нормальный полёт** (error < 0.3м)
```cpp
start_pt_ = planned_pos;    // Используем плановую позицию
start_vel_ = planned_vel;   // (как было раньше)
```

**Результат:** Стандартное поведение, всё работает как прежде

---

## 🎬 ЧТО ТЕПЕРЬ ПРОИЗОЙДЁТ

### **Сценарий: Дрон с выключенными моторами**

```
t=0.0с: Планировщик создаёт траекторию
        🟢 Зелёный = (0, 0, 1)
        🔴 Красный = (0, 0, 1)
        ✅ Совпадают

t=1.0с: Дрон стоит, траектория начинает "убегать"
        🟢 Зелёный = (0, 0, 1)  [скорость: 0.0 м/с]
        🔴 Красный = (2, 0, 1)
        ⚠️  Отклонение: 2.0м, скорость: 0.0 м/с
        
        🛑 ТРИГГЕР! tracking_error > 1.0 && velocity < 0.1
        → Перепланирование с РЕАЛЬНОЙ позицией

t=1.1с: Новая траектория создана
        🟢 Зелёный = (0, 0, 1)
        🔴 Красный = (0, 0, 1)  ← Вернулся к дрону!
        🟠 Оранжевая = начинается от (0, 0, 1)
        ✅ Траектория "привязана" к дрону!

t=2.0с: Дрон всё ещё стоит
        🟢 Зелёный = (0, 0, 1)
        🔴 Красный = (2, 0, 1)  ← Опять убежал
        
        🛑 ТРИГГЕР! Снова перепланирование
        → Траектория снова вернётся к дрону

t=5.0с: Моторы включены, дрон в OFFBOARD
        🟢 Зелёный начинает двигаться = (0.5, 0, 1)
        🔴 Красный = (0.6, 0, 1)
        ✅ Отклонение маленькое (0.1м)
        ✅ Скорость > 0.1 м/с
        → Нормальный полёт, триггер НЕ срабатывает
```

---

## 📊 ПАРАМЕТРЫ

Вы можете настроить пороги в коде:

### В `EXEC_TRAJ` (детектирование застревания):
```cpp
if (tracking_error > 1.0 && velocity_magnitude < 0.1)
     // ↑ Порог ошибки      ↑ Порог скорости
```

**Рекомендации:**
- `tracking_error`: 0.8 - 1.5м (чем меньше, тем чаще перепланирование)
- `velocity_magnitude`: 0.05 - 0.2 м/с (скорость "считается нулевой")

### В `planFromCurrentTraj()` (использование одометрии):
```cpp
if (tracking_error > 0.8 && velocity_magnitude < 0.15)
     // ↑ Застрял           ↑ Не двигается

else if (tracking_error > 0.3 && tracking_error <= 0.8)
         // ↑ Умеренное отклонение
```

**Рекомендации:**
- Критическое: 0.5 - 1.0м
- Умеренное: 0.2 - 0.5м
- Blend коэффициент: 0.5 - 0.8 (больше = больше доверия одометрии)

---

## ✅ ПРЕИМУЩЕСТВА ЭТОГО ПОДХОДА

1. ✅ **Автоматическая адаптация**
   - Если дрон не движется → траектория привязана к нему
   - Если дрон летит нормально → стандартное поведение

2. ✅ **Плавность**
   - Нет резких скачков траектории
   - Плавное смешивание при умеренных отклонениях

3. ✅ **Безопасность**
   - Траектория никогда не "убежит" далеко
   - При включении моторов дрон не будет пытаться "догнать" траекторию

4. ✅ **Диагностика**
   - Предупреждения в логах
   - Видно в RViz (зелёный и красный совпадают)

5. ✅ **Обратная совместимость**
   - При нормальном полёте работает как раньше
   - Изменения активируются только когда нужно

---

## 🧪 ТЕСТИРОВАНИЕ

### Тест 1: Дрон с выключенными моторами
1. Запустить планировщик
2. Дать цель (2D Nav Goal)
3. **Ожидание:** 
   - 🟢 Зелёный маркер стоит на месте
   - 🔴 Красный НЕ убегает дальше 1м
   - 🟠 Оранжевая траектория "прилипает" к дрону
   - В логах: "Drone not moving but trajectory running!"

### Тест 2: Дрон в нормальном полёте
1. Запустить планировщик
2. Переключить в OFFBOARD
3. Дать цель
4. **Ожидание:**
   - 🟢 Зелёный движется
   - 🔴 Красный движется рядом (< 30см)
   - Нормальный полёт без предупреждений

### Тест 3: Дрон застрял на препятствии
1. Запустить планировщик в симуляции
2. Дрон летит в стену
3. **Ожидание:**
   - 🟢 Зелёный застрял у стены
   - 🔴 Красный пытается продолжить
   - Отклонение растёт → Перепланирование
   - Новая траектория огибает препятствие

---

## 🔧 ЕСЛИ НУЖНА ТОНКАЯ НАСТРОЙКА

### Сделать более чувствительным (частые перепланирования):
```cpp
// В EXEC_TRAJ:
if (tracking_error > 0.5 && velocity_magnitude < 0.15)  // Было 1.0 и 0.1

// В planFromCurrentTraj:
if (tracking_error > 0.4 && velocity_magnitude < 0.2)  // Было 0.8 и 0.15
```

### Сделать менее чувствительным (редкие перепланирования):
```cpp
// В EXEC_TRAJ:
if (tracking_error > 1.5 && velocity_magnitude < 0.05)  // Было 1.0 и 0.1

// В planFromCurrentTraj:
if (tracking_error > 1.2 && velocity_magnitude < 0.1)  // Было 0.8 и 0.15
```

### Изменить степень смешивания:
```cpp
double blend = 0.7;  // Больше доверия одометрии (было 0.6)
double blend = 0.4;  // Больше доверия траектории (было 0.6)
```

---

## 📝 ЛОГИ ДЛЯ МОНИТОРИНГА

Вы будете видеть:

```bash
# Когда дрон застрял:
[ WARN] 🛑 Drone not moving but trajectory running! Error: 1.24m, vel: 0.03m/s
[ WARN]    Possible: not in OFFBOARD mode or motors disabled. Replanning from current position...
[ WARN] 🔧 Using REAL odometry for replanning (drone not moving, error: 1.24m)

# Умеренное отклонение:
[ INFO] 🔀 Blending real and planned position (error: 0.45m)

# Нормальный полёт - логов нет (всё хорошо)
```

---

## 🎯 ИТОГ

**Теперь ваша проблема решена:**
- ✅ Траектория НЕ убегает от дрона
- ✅ Визуализация всегда показывает актуальную траекторию
- ✅ Безопасный запуск даже с выключенными моторами
- ✅ Плавный полёт без рывков

**Скомпилируйте и протестируйте!** 🚀
