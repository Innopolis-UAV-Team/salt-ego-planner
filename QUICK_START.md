# 🚀 Быстрая инструкция: Что делать ПРЯМО СЕЙЧАС

## ✅ ШАГ 1: Компиляция и запуск (5 минут)

```bash
cd ~/catkin_ws  # или где у вас workspace
catkin_make
source devel/setup.bash
roslaunch ego_planner run_in_sim.launch  # или ваш launch файл
```

## ✅ ШАГ 2: Настройка RViz (2 минуты)

Добавьте новые топики в RViz:

1. **Marker** → Topic: `/ego_planner_node/debug/current_odom`
   - 🟢 **Зелёный** - где дрон РЕАЛЬНО находится

2. **Marker** → Topic: `/ego_planner_node/debug/planned_position`
   - 🔴 **Красный** - где дрон ДОЛЖЕН быть по траектории

3. **Marker** → Topic: `/ego_planner_node/debug/dense_trajectory`
   - 🟠 **Оранжевый** - плотная визуализация траектории

## 🔍 ШАГ 3: Смотрите и анализируйте (10 минут)

### ✅ ХОРОШО (всё работает правильно):
```
🟢 Зелёный и 🔴 красный почти совпадают (разница < 20см)
🟠 Оранжевая линия плавная
Дрон летит стабильно
```
**→ Вывод:** Проблема только в визуализации контрольных точек.
**→ Решение:** Используйте оранжевую траекторию, всё работает!

---

### ⚠️ ПРОБЛЕМА (дрон отстаёт):
```
🟢 Зелёный ОТСТАЁТ от 🔴 красного (разница > 30см)
Дрон двигается медленнее чем планировалось
```
**→ Вывод:** Дрон не успевает за траекторией.
**→ Решение:** Смотрите файл `SOLUTION_PLAN.md` → ШАГ 2А

---

### ❌ СЕРЬЁЗНАЯ ПРОБЛЕМА (шумная одометрия):
```
🟢 Зелёный "прыгает" хаотично
Красный двигается плавно
```
**→ Вывод:** Проблема в одометрии (лидар шумит).
**→ Решение:** Проверить настройки лидара, фильтрацию

---

## 📊 ШАГ 4: Запись данных для анализа

```bash
# Записать rosbag (5-10 минут полёта)
rosbag record /ego_planner_node/debug/current_odom \
              /ego_planner_node/debug/planned_position \
              /odom_world \
              -O diagnostic_flight.bag

# Потом проанализировать:
rostopic echo -b diagnostic_flight.bag /ego_planner_node/debug/current_odom
```

---

## 🎯 ЕСЛИ УВИДЕЛИ ПРОБЛЕМУ - СЛЕДУЮЩИЕ ШАГИ:

### Вариант А: "Дрон отстаёт" (30 минут)

1. Откройте `SOLUTION_CODE_ADAPTIVE_REPLAN.cpp`
2. Скопируйте код в `ego_replan_fsm.cpp`
3. Добавьте параметр в `advanced_param.xml`:
   ```xml
   <param name="fsm/max_tracking_error" value="0.5" type="double"/>
   ```
4. Перекомпилируйте и тестируйте

### Вариант Б: "Дрон летит слишком быстро" (1 минута)

В `run_in_sim.launch` уменьшите скорость:
```xml
<arg name="max_vel" value="1.5" />  <!-- Было 2.0 -->
<arg name="max_acc" value="2.0" />  <!-- Было 3.0 -->
```

### Вариант В: "Всё хорошо, проблема только визуализация" (0 минут)

**НИЧЕГО НЕ ДЕЛАЙТЕ!** Просто используйте оранжевую траекторию в RViz.

---

## 📋 Контрольный список

- [ ] Скомпилировал с новым кодом
- [ ] Добавил топики в RViz (зелёный, красный, оранжевый)
- [ ] Запустил полёт
- [ ] Записал rosbag
- [ ] Определил сценарий (хорошо/отстаёт/шумит)
- [ ] Применил нужное решение
- [ ] Повторно протестировал

---

## ❓ Частые вопросы

**Q: Не вижу маркеры в RViz**
- Проверьте что топики публикуются: `rostopic list | grep debug`
- Проверьте Fixed Frame в RViz: должен быть "world"

**Q: Маркеры есть но не двигаются**
- Проверьте что дрон в режиме EXEC_TRAJ (не WAIT_TARGET)
- Дайте цель через 2D Nav Goal в RViz

**Q: Зелёный и красный постоянно на расстоянии 50см+**
- ⚠️ Это проблема! Дрон не успевает.
- Уменьшите max_vel или добавьте проверку отклонения

**Q: Компиляция выдаёт ошибки**
- Проверьте что вы изменили все 3 файла:
  - `planning_visualization.h`
  - `planning_visualization.cpp`
  - `ego_replan_fsm.cpp`

---

## 🆘 Если что-то сломалось

```bash
# Откатить изменения
cd ~/catkin_ws/src/salt-ego-planner
git checkout .

# Или восстановить конкретный файл
git checkout src/planner/plan_manage/src/ego_replan_fsm.cpp
```

---

## 📞 Результаты

После тестов напишите:
1. Какой сценарий увидели? (А/Б/В)
2. Среднее расстояние зелёный-красный?
3. Дрон летает стабильно?

И мы определим следующие шаги! 🚀
